# Env
.env

# Go
/backend/server
/backend/seed
*.exe

# Node / SvelteKit
node_modules/
/frontend/.svelte-kit/
/frontend/build/
/frontend/.vite/

# Local seed-data overrides (the committed defaults are data/areas.json and
# data/creatures.sample.json; a ".local" file overrides without editing them).
/data/areas.local.json
/data/creatures.local.json

# Editor / OS
.DS_Store
Thumbs.db
